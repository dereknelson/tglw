# x402 Checkout + Apliiq Fulfillment Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Wire up x402 crypto payments (USDC on Base mainnet) and Apliiq order fulfillment so both AI agents and humans with wallets can buy the TGLW shirt.

**Architecture:** Single x402-protected API route (`POST /api/checkout`) handles payment verification and settlement via the x402 facilitator, then creates an Apliiq fulfillment order. Frontend adds a checkout form (size + shipping) and wallet connect for human buyers. Agent discovery via `/llms.txt` and `/.well-known/x402.json` at the root.

**Tech Stack:** @x402/core + @x402/evm (server-side payment), wagmi + viem + @x402/fetch (client wallet connect + payment), Apliiq REST API (HMAC auth, order fulfillment), TanStack Start server routes

---

### Task 1: Install dependencies

**Files:**

- Modify: `package.json`

**Step 1: Install x402 packages**

```bash
npm install @x402/core @x402/evm
```

**Step 2: Install client-side wallet packages**

```bash
npm install @x402/fetch wagmi viem @tanstack/react-query @rainbow-me/rainbowkit
```

**Step 3: Verify build**

```bash
npm run build
```

Expected: Clean build, no errors.

**Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add x402, wagmi, viem, rainbowkit dependencies"
```

---

### Task 2: Agent discovery files — llms.txt and .well-known/x402.json

**Files:**

- Create: `public/llms.txt`
- Create: `public/.well-known/x402.json`

**Step 1: Create `public/llms.txt`**

```markdown
# TGLW — Lift Weights Touch Grass

## Product

Black tee with palm tree + sun graphic. "LIFT WEIGHTS TOUCH GRASS."
100% cotton. $35 USD.

## Sizes

S, M, L, XL, 2XL

## How to Buy

POST /api/checkout
Payment: $35 USDC on Base (x402 protocol)

Request body:
{
"shipping": {
"name": "Your Name",
"address1": "123 Main St",
"city": "Los Angeles",
"state": "CA",
"zip": "90001",
"country": "US"
},
"size": "L"
}

The endpoint is x402-protected. Send your payment signature in the Payment-Signature header.
On success, you'll receive an order confirmation with tracking details.

## Structured Data

See /.well-known/x402.json for machine-parseable product and payment info.
```

**Step 2: Create `public/.well-known/x402.json`**

```json
{
  "name": "TGLW — Lift Weights Touch Grass",
  "description": "Single-product t-shirt store powered by x402 payments",
  "products": [
    {
      "id": "tglw-black-tee",
      "name": "Lift Weights Touch Grass Tee",
      "description": "Black tee with palm tree + sun graphic",
      "price": "35.00",
      "currency": "USDC",
      "network": "eip155:8453",
      "sizes": ["S", "M", "L", "XL", "2XL"],
      "checkout": {
        "endpoint": "/api/checkout",
        "method": "POST",
        "protocol": "x402",
        "body_schema": {
          "shipping": {
            "name": "string",
            "address1": "string",
            "city": "string",
            "state": "string",
            "zip": "string",
            "country": "string (ISO 2-letter)"
          },
          "size": "string (S|M|L|XL|2XL)"
        }
      }
    }
  ]
}
```

**Step 3: Verify the files are served**

```bash
npm run dev
# In another terminal:
curl http://localhost:3000/llms.txt
curl http://localhost:3000/.well-known/x402.json
```

Expected: Both files served with correct content.

**Step 4: Commit**

```bash
git add public/llms.txt public/.well-known/x402.json
git commit -m "feat: add llms.txt and .well-known/x402.json for agent discovery"
```

---

### Task 3: Apliiq client module

**Files:**

- Create: `src/server/apliiq.ts`

**Step 1: Create the Apliiq client**

This module handles HMAC authentication and the Create Order API call. The Apliiq API uses a custom `x-apliiq-auth` header with the format `RTS:SIG:APPID:STATE`.

```typescript
import { createHmac } from 'node:crypto'

const APLIIQ_BASE = 'https://api.apliiq.com/api'

function buildAuthHeader(method: string, path: string, body: string): string {
  const appKey = process.env.APLIIQ_APP_KEY!
  const sharedSecret = process.env.APLIIQ_SHARED_SECRET!
  const rts = Math.floor(Date.now() / 1000).toString()
  const state = '' // empty for stateless calls

  // HMAC signature: method + path + body + timestamp
  const message = `${method}${path}${body}${rts}`
  const sig = createHmac('sha256', sharedSecret)
    .update(message)
    .digest('base64')

  return `${rts}:${sig}:${appKey}:${state}`
}

async function apliiqRequest(
  method: string,
  path: string,
  body?: Record<string, unknown>,
): Promise<unknown> {
  const bodyStr = body ? JSON.stringify(body) : ''
  const authHeader = buildAuthHeader(method, path, bodyStr)

  const res = await fetch(`${APLIIQ_BASE}${path}`, {
    method,
    headers: {
      'Content-Type': 'application/json',
      'x-apliiq-auth': authHeader,
    },
    ...(body ? { body: bodyStr } : {}),
  })

  if (!res.ok) {
    const text = await res.text()
    throw new Error(`Apliiq API error ${res.status}: ${text}`)
  }

  return res.json()
}

export interface ShippingInfo {
  name: string
  address1: string
  city: string
  state: string
  zip: string
  country: string
}

export interface CreateOrderResult {
  orderId: string
  status: string
}

export async function createOrder(
  shipping: ShippingInfo,
  size: string,
): Promise<CreateOrderResult> {
  // NOTE: The product_id and variant details need to be configured
  // after the Apliiq product is set up. Using placeholder for now.
  const orderData = {
    shipping_address: {
      first_name: shipping.name.split(' ')[0] || shipping.name,
      last_name: shipping.name.split(' ').slice(1).join(' ') || '',
      address1: shipping.address1,
      city: shipping.city,
      province: shipping.state,
      zip: shipping.zip,
      country: shipping.country,
    },
    line_items: [
      {
        quantity: 1,
        size,
        // TODO: Set these after Apliiq product setup
        product_id: process.env.APLIIQ_PRODUCT_ID || 'PLACEHOLDER',
      },
    ],
  }

  const result = (await apliiqRequest('POST', '/Order/', orderData)) as {
    id?: string
    order_id?: string
    status?: string
  }

  return {
    orderId: result.id || result.order_id || 'unknown',
    status: result.status || 'submitted',
  }
}

export async function getProducts(): Promise<unknown> {
  return apliiqRequest('GET', '/Product/')
}
```

**Step 2: Verify it compiles**

```bash
npm run build
```

**Step 3: Commit**

```bash
git add src/server/apliiq.ts
git commit -m "feat: Apliiq client module with HMAC auth and order creation"
```

---

### Task 4: x402-protected checkout API route

**Files:**

- Create: `src/routes/api/checkout.ts`

This is the core endpoint. It manually implements the x402 flow using `@x402/core` since TanStack Start uses standard Web Request/Response (not Express).

**Step 1: Create the checkout route**

```typescript
import { createFileRoute } from '@tanstack/react-router'
import { createOrder, type ShippingInfo } from '../../server/apliiq'

const PAY_TO = process.env.X402_PAY_TO!
const FACILITATOR_URL = 'https://x402.org/facilitator'

// USDC on Base mainnet
const USDC_BASE = '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913'
const NETWORK = 'eip155:8453'

// $35.00 in USDC base units (6 decimals)
const PRICE_AMOUNT = '35000000'

const VALID_SIZES = ['S', 'M', 'L', 'XL', '2XL']

const paymentRequirements = {
  scheme: 'exact',
  network: NETWORK,
  asset: USDC_BASE,
  amount: PRICE_AMOUNT,
  payTo: PAY_TO,
  maxTimeoutSeconds: 300,
  description: 'TGLW — Lift Weights Touch Grass Black Tee — $35',
  extra: {},
}

export const Route = createFileRoute('/api/checkout')({
  server: {
    handlers: {
      POST: async ({ request }) => {
        // Parse body first to validate before payment
        let body: { shipping?: ShippingInfo; size?: string }
        try {
          body = await request.json()
        } catch {
          return Response.json({ error: 'Invalid JSON body' }, { status: 400 })
        }

        // Validate required fields
        const { shipping, size } = body
        if (!shipping || !size) {
          return Response.json(
            { error: 'Missing required fields: shipping, size' },
            { status: 400 },
          )
        }

        if (!VALID_SIZES.includes(size)) {
          return Response.json(
            {
              error: `Invalid size. Must be one of: ${VALID_SIZES.join(', ')}`,
            },
            { status: 400 },
          )
        }

        if (
          !shipping.name ||
          !shipping.address1 ||
          !shipping.city ||
          !shipping.state ||
          !shipping.zip ||
          !shipping.country
        ) {
          return Response.json(
            {
              error:
                'Missing shipping fields: name, address1, city, state, zip, country',
            },
            { status: 400 },
          )
        }

        // Check for x402 payment signature
        const paymentSignature =
          request.headers.get('Payment-Signature') ||
          request.headers.get('X-Payment')

        if (!paymentSignature) {
          // No payment — return 402 with payment requirements
          const paymentRequired = btoa(
            JSON.stringify({
              x402Version: 2,
              accepts: [paymentRequirements],
            }),
          )

          return new Response(
            JSON.stringify({
              error: 'Payment required',
              description: paymentRequirements.description,
              price: '$35.00 USDC',
              network: 'Base',
            }),
            {
              status: 402,
              headers: {
                'Content-Type': 'application/json',
                'Payment-Required': paymentRequired,
              },
            },
          )
        }

        // Verify payment with facilitator
        let paymentPayload: Record<string, unknown>
        try {
          paymentPayload = JSON.parse(atob(paymentSignature))
        } catch {
          return Response.json(
            { error: 'Invalid payment signature encoding' },
            { status: 400 },
          )
        }

        const verifyRes = await fetch(`${FACILITATOR_URL}/verify`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            x402Version: paymentPayload.x402Version ?? 2,
            paymentPayload,
            paymentRequirements,
          }),
        })

        const verifyResult = (await verifyRes.json()) as {
          isValid: boolean
          invalidReason?: string
        }

        if (!verifyResult.isValid) {
          return Response.json(
            {
              error: 'Payment verification failed',
              reason: verifyResult.invalidReason,
            },
            { status: 402 },
          )
        }

        // Payment verified — create Apliiq order
        let orderResult: { orderId: string; status: string }
        try {
          orderResult = await createOrder(shipping, size)
        } catch (err) {
          // Payment was verified but fulfillment failed — log this for manual resolution
          console.error('Apliiq order creation failed:', err)
          return Response.json(
            {
              error:
                'Order fulfillment failed. Payment was verified. Contact support.',
            },
            { status: 500 },
          )
        }

        // Settle payment with facilitator
        const settleRes = await fetch(`${FACILITATOR_URL}/settle`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            x402Version: paymentPayload.x402Version ?? 2,
            paymentPayload,
            paymentRequirements,
          }),
        })

        const settleResult = (await settleRes.json()) as {
          success: boolean
          transaction?: string
          errorReason?: string
        }

        if (!settleResult.success) {
          console.error('Payment settlement failed:', settleResult.errorReason)
          // Order was created but settlement failed — still fulfill but log
        }

        const responseBody = {
          order_id: orderResult.orderId,
          status: orderResult.status,
          tx_hash: settleResult.transaction || null,
          message: 'Your shirt is on the way.',
        }

        const paymentResponse = btoa(
          JSON.stringify({
            success: settleResult.success,
            transaction: settleResult.transaction,
          }),
        )

        return new Response(JSON.stringify(responseBody), {
          status: 200,
          headers: {
            'Content-Type': 'application/json',
            'Payment-Response': paymentResponse,
          },
        })
      },
    },
  },
})
```

**Step 2: Verify build**

```bash
npm run build
```

**Step 3: Commit**

```bash
git add src/routes/api/checkout.ts
git commit -m "feat: x402-protected POST /api/checkout endpoint with Apliiq fulfillment"
```

---

### Task 5: Wallet provider setup for frontend

**Files:**

- Create: `src/components/WalletProvider.tsx`
- Modify: `src/routes/__root.tsx`

**Step 1: Create the wallet provider**

This wraps the app with wagmi + RainbowKit + React Query for wallet connection.

```tsx
import { WagmiProvider, createConfig, http } from 'wagmi'
import { base } from 'wagmi/chains'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { RainbowKitProvider, getDefaultConfig } from '@rainbow-me/rainbowkit'
import '@rainbow-me/rainbowkit/styles.css'

const config = getDefaultConfig({
  appName: 'TGLW',
  projectId: 'tglw-store', // WalletConnect project ID — get from cloud.walletconnect.com for production
  chains: [base],
  transports: {
    [base.id]: http(),
  },
})

const queryClient = new QueryClient()

export default function WalletProvider({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <WagmiProvider config={config}>
      <QueryClientProvider client={queryClient}>
        <RainbowKitProvider>{children}</RainbowKitProvider>
      </QueryClientProvider>
    </WagmiProvider>
  )
}
```

**Step 2: Wrap the app in the wallet provider**

In `src/routes/__root.tsx`, import `WalletProvider` and wrap `{children}`:

```tsx
import { HeadContent, Scripts, createRootRoute } from '@tanstack/react-router'
import Header from '../components/Header'
import WalletProvider from '../components/WalletProvider'

import appCss from '../styles.css?url'

export const Route = createRootRoute({
  head: () => ({
    meta: [
      { charSet: 'utf-8' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' },
      { title: 'TGLW — Lift Weights Touch Grass' },
    ],
    links: [{ rel: 'stylesheet', href: appCss }],
  }),
  shellComponent: RootDocument,
})

function RootDocument({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        <HeadContent />
      </head>
      <body className="font-sans antialiased">
        <WalletProvider>
          <Header />
          {children}
        </WalletProvider>
        <Scripts />
      </body>
    </html>
  )
}
```

**Step 3: Verify build**

```bash
npm run build
```

**Step 4: Commit**

```bash
git add src/components/WalletProvider.tsx src/routes/__root.tsx
git commit -m "feat: wallet provider — wagmi + RainbowKit on Base"
```

---

### Task 6: Checkout form component

**Files:**

- Create: `src/components/CheckoutForm.tsx`

**Step 1: Create the checkout form**

A modal/overlay form that collects size and shipping info, then triggers the x402 payment via the connected wallet.

```tsx
import { useState } from 'react'
import { useAccount } from 'wagmi'
import { ConnectButton } from '@rainbow-me/rainbowkit'

const VALID_SIZES = ['S', 'M', 'L', 'XL', '2XL'] as const

interface CheckoutFormProps {
  onClose: () => void
}

export default function CheckoutForm({ onClose }: CheckoutFormProps) {
  const { address, isConnected } = useAccount()
  const [size, setSize] = useState<string>('L')
  const [shipping, setShipping] = useState({
    name: '',
    address1: '',
    city: '',
    state: '',
    zip: '',
    country: 'US',
  })
  const [status, setStatus] = useState<'idle' | 'paying' | 'success' | 'error'>(
    'idle',
  )
  const [error, setError] = useState('')
  const [orderResult, setOrderResult] = useState<{
    order_id: string
    tx_hash: string | null
  } | null>(null)

  function updateShipping(field: string, value: string) {
    setShipping((prev) => ({ ...prev, [field]: value }))
  }

  async function handleCheckout() {
    if (!isConnected) return

    setStatus('paying')
    setError('')

    try {
      // Import dynamically to avoid SSR issues
      const { wrapFetchWithPayment } = await import('@x402/fetch')
      const { createX402Client } = await import('@x402/evm/client')

      // Create x402 client with the connected wallet
      const x402Client = createX402Client(address!)
      const fetchWithPayment = wrapFetchWithPayment(fetch, x402Client)

      const res = await fetchWithPayment('/api/checkout', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ shipping, size }),
      })

      if (!res.ok) {
        const data = await res.json()
        throw new Error(data.error || `Request failed: ${res.status}`)
      }

      const data = await res.json()
      setOrderResult(data)
      setStatus('success')
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Checkout failed')
      setStatus('error')
    }
  }

  const inputClass =
    'w-full rounded-lg border border-[var(--line)] bg-[var(--surface)] px-3 py-2.5 text-sm text-[var(--ink)] outline-none focus:border-[var(--ink)] transition'

  if (status === 'success' && orderResult) {
    return (
      <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 px-4">
        <div className="rise-in w-full max-w-sm rounded-2xl bg-[var(--surface)] p-8 text-center shadow-xl">
          <div className="mb-4 text-4xl">&#10003;</div>
          <h2 className="mb-2 text-xl font-semibold text-[var(--ink)]">
            Order Confirmed
          </h2>
          <p className="mb-1 text-sm text-[var(--ink-soft)]">
            Order #{orderResult.order_id}
          </p>
          {orderResult.tx_hash && (
            <p className="mb-4 text-xs text-[var(--ink-muted)]">
              tx: {orderResult.tx_hash.slice(0, 10)}...
            </p>
          )}
          <p className="mb-6 text-sm text-[var(--ink-soft)]">
            Your shirt is on the way.
          </p>
          <button
            type="button"
            onClick={onClose}
            className="rounded-full bg-[var(--accent)] px-6 py-2.5 text-sm font-semibold text-white transition hover:bg-[var(--accent-hover)]"
          >
            Done
          </button>
        </div>
      </div>
    )
  }

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 px-4">
      <div className="rise-in w-full max-w-sm rounded-2xl bg-[var(--surface)] p-6 shadow-xl">
        <div className="mb-6 flex items-center justify-between">
          <h2 className="text-lg font-semibold text-[var(--ink)]">Checkout</h2>
          <button
            type="button"
            onClick={onClose}
            className="text-[var(--ink-muted)] hover:text-[var(--ink)]"
          >
            &times;
          </button>
        </div>

        {/* Size selector */}
        <div className="mb-5">
          <label className="mb-2 block text-xs font-semibold uppercase tracking-widest text-[var(--ink-muted)]">
            Size
          </label>
          <div className="flex gap-2">
            {VALID_SIZES.map((s) => (
              <button
                key={s}
                type="button"
                onClick={() => setSize(s)}
                className={`flex-1 rounded-lg border py-2 text-sm font-medium transition ${
                  size === s
                    ? 'border-[var(--ink)] bg-[var(--ink)] text-white'
                    : 'border-[var(--line)] text-[var(--ink-soft)] hover:border-[var(--ink-muted)]'
                }`}
              >
                {s}
              </button>
            ))}
          </div>
        </div>

        {/* Shipping fields */}
        <div className="mb-5 space-y-3">
          <label className="mb-2 block text-xs font-semibold uppercase tracking-widest text-[var(--ink-muted)]">
            Shipping
          </label>
          <input
            type="text"
            placeholder="Full name"
            value={shipping.name}
            onChange={(e) => updateShipping('name', e.target.value)}
            className={inputClass}
          />
          <input
            type="text"
            placeholder="Address"
            value={shipping.address1}
            onChange={(e) => updateShipping('address1', e.target.value)}
            className={inputClass}
          />
          <div className="flex gap-2">
            <input
              type="text"
              placeholder="City"
              value={shipping.city}
              onChange={(e) => updateShipping('city', e.target.value)}
              className={inputClass}
            />
            <input
              type="text"
              placeholder="State"
              value={shipping.state}
              onChange={(e) => updateShipping('state', e.target.value)}
              className={`${inputClass} w-20`}
            />
          </div>
          <div className="flex gap-2">
            <input
              type="text"
              placeholder="ZIP"
              value={shipping.zip}
              onChange={(e) => updateShipping('zip', e.target.value)}
              className={inputClass}
            />
            <input
              type="text"
              placeholder="Country"
              value={shipping.country}
              onChange={(e) => updateShipping('country', e.target.value)}
              className={`${inputClass} w-20`}
            />
          </div>
        </div>

        {/* Wallet connect + pay */}
        <div className="space-y-3">
          {!isConnected ? (
            <div className="flex justify-center">
              <ConnectButton />
            </div>
          ) : (
            <button
              type="button"
              onClick={handleCheckout}
              disabled={
                status === 'paying' ||
                !shipping.name ||
                !shipping.address1 ||
                !shipping.city ||
                !shipping.zip
              }
              className="w-full cursor-pointer rounded-full bg-[var(--accent)] px-8 py-3.5 text-sm font-semibold uppercase tracking-[0.15em] text-white transition hover:bg-[var(--accent-hover)] hover:scale-[1.02] active:scale-[0.98] disabled:cursor-not-allowed disabled:opacity-50"
            >
              {status === 'paying' ? 'Processing...' : 'Pay $35 USDC'}
            </button>
          )}

          {error && <p className="text-center text-sm text-red-500">{error}</p>}
        </div>

        <p className="mt-4 text-center text-xs text-[var(--ink-muted)]">
          $35 USDC on Base · Powered by x402
        </p>
      </div>
    </div>
  )
}
```

**Step 2: Verify build**

```bash
npm run build
```

**Step 3: Commit**

```bash
git add src/components/CheckoutForm.tsx
git commit -m "feat: checkout form — size selector, shipping, wallet connect, x402 payment"
```

---

### Task 7: Wire up the Buy Now button

**Files:**

- Modify: `src/routes/index.tsx`

**Step 1: Update the storefront to open the checkout form**

```tsx
import { createFileRoute } from '@tanstack/react-router'
import { useState } from 'react'
import CheckoutForm from '../components/CheckoutForm'

export const Route = createFileRoute('/')({ component: Store })

function Store() {
  const [showCheckout, setShowCheckout] = useState(false)

  return (
    <main className="flex min-h-svh flex-col items-center justify-center px-6 pt-20 pb-12">
      <div className="rise-in flex w-full max-w-md flex-col items-center text-center">
        {/* Shirt mockup placeholder */}
        <div className="relative mb-10 aspect-square w-full max-w-sm">
          <div className="flex h-full w-full items-center justify-center rounded-2xl bg-[var(--surface)] shadow-[0_2px_40px_rgba(0,0,0,0.06)]">
            <div className="flex flex-col items-center gap-3 text-[var(--ink-muted)]">
              <svg
                width="64"
                height="64"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                strokeWidth="1"
                strokeLinecap="round"
                strokeLinejoin="round"
              >
                <path d="M20.38 3.46 16 2 12 5 8 2 3.62 3.46a2 2 0 0 0-1.34 1.88v1.14a2 2 0 0 0 .78 1.58L7 11v10h10V11l3.94-2.94a2 2 0 0 0 .78-1.58V5.34a2 2 0 0 0-1.34-1.88Z" />
              </svg>
              <span className="text-xs font-medium uppercase tracking-widest">
                Shirt Mockup
              </span>
            </div>
          </div>
        </div>

        {/* Product info */}
        <h1 className="mb-3 font-[var(--font-serif)] text-3xl font-medium tracking-tight text-[var(--ink)] sm:text-4xl">
          Lift Weights Touch Grass
        </h1>

        <p className="mb-1 text-sm text-[var(--ink-soft)]">
          Black tee · 100% cotton
        </p>

        <p className="mb-8 text-xl font-semibold text-[var(--ink)]">$35</p>

        <button
          type="button"
          onClick={() => setShowCheckout(true)}
          className="w-full max-w-xs cursor-pointer rounded-full bg-[var(--accent)] px-8 py-4 text-sm font-semibold uppercase tracking-[0.15em] text-white transition hover:bg-[var(--accent-hover)] hover:scale-[1.02] active:scale-[0.98]"
        >
          Buy Now
        </button>

        <p className="mt-4 text-xs text-[var(--ink-muted)]">Powered by x402</p>
      </div>

      {showCheckout && <CheckoutForm onClose={() => setShowCheckout(false)} />}
    </main>
  )
}
```

**Step 2: Verify build**

```bash
npm run build
```

**Step 3: Commit**

```bash
git add src/routes/index.tsx
git commit -m "feat: wire Buy Now button to checkout form"
```

---

### Task 8: Lint, build, and push

**Step 1: Run lint and format**

```bash
npm run check
```

Fix any issues.

**Step 2: Production build**

```bash
npm run build
```

**Step 3: Commit fixes if any**

```bash
git add -A
git commit -m "chore: lint and format fixes"
```

**Step 4: Push**

```bash
git push
```

---

## Notes

- **Apliiq product setup**: After Task 3, you'll need to create the actual product in Apliiq (via their dashboard or API) and set the `APLIIQ_PRODUCT_ID` env var. The HMAC auth format may need adjustment once we can read their full API docs in a browser.
- **WalletConnect project ID**: The `projectId` in the wallet config is a placeholder. For production, register at cloud.walletconnect.com to get a real project ID.
- **x402/fetch client integration**: The `@x402/fetch` + `@x402/evm/client` imports in the CheckoutForm may need adjustment based on the actual package exports. These packages are relatively new — if the imports don't resolve, we fall back to manually constructing the payment signature with viem.
- **Testing the x402 flow**: You can test with Base Sepolia first by changing `NETWORK` to `eip155:84532` and using testnet USDC. The open facilitator at `x402.org/facilitator` supports testnet.
